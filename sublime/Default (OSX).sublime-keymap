[
    // Key Binding for creating a file (CP New)
    // { "keys": ["option+shift+n"], "command": "cp_new" },

    // Key Binding for creating a set of file (CP Set)
    // { "keys": ["option+shift+s"], "command": "cp_set" },

    // Key Binding for opening a file (CP Open)
    // { "keys": ["option+shift+o"], "command": "cp_open" },

    // Key Binding for exiting CP mode (CP End)
    // { "keys": ["option+shift+e"], "command": "cp_end" },
]
