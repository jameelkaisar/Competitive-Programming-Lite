#include <stdio.h>
#include <stdlib.h>
typedef long long ll;


void solution() {
    // Solution {N}
}


int main() {
    #ifndef ONLINE_JUDGE
    freopen(".cpl_files/{N}.in.txt", "r", stdin);
    freopen(".cpl_files/{N}.out.txt", "w", stdout);
    #endif
    ll t = 0;
    scanf("%lld", &t);
    while (t--) solution();
    return 0;
}
